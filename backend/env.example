DATABASE_URL=postgresql://username:password@localhost:5432/store_flow_analytics
DB_HOST=localhost
DB_PORT=5432
DB_NAME=store_flow_analytics
DB_USER=your_username
DB_PASSWORD=your_password

# FastAPI settings
DEBUG=True
API_PREFIX=/api

# Logging Configuration
# LOG_LEVEL options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Databricks Configuration (for local development)
# These are automatically set in Databricks Apps environment
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_TOKEN=your_personal_access_token

# Databricks Apps Environment Variables (automatically set by runtime)
# DATABRICKS_CLIENT_ID=auto_set_by_databricks_apps
# DATABRICKS_CLIENT_SECRET=auto_set_by_databricks_apps 